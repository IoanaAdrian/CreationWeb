//Form validation  + link validation (if testdir contains a ? don't even test it)

--> Custom JSON parser (you will have a json file where you can configure constant things: folderName, hostName)
//- design
- error handling refactor
- auth
- upload
- sys resource management